{
  "name": "honorbox",
  "version": "0.2.0",
  "description": "Sell digital products with just Stripe + GitHub. No platform, no monthly fee, no server.",
  "license": "MIT",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test scripts/test/core.test.js scripts/test/dispatch.test.js scripts/test/fulfill-driver.test.js",
    "build": "node scripts/build.js"
  }
}
